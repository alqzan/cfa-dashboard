{
  "name": "cfa-dashboard",
  "private": true,
  "description": "CFA Level II Personal Coach dashboard (static, GitHub Pages)",
  "scripts": {
    "test": "for f in tests/*.test.mjs; do node \"$f\" || exit 1; done",
    "test:browser": "node tests/browser/sync-and-copy.e2e.mjs"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
