# المزامنة بين الأجهزة — خطوات الإعداد (مرة واحدة، ~5 دقائق)

الموقع ثابت (GitHub Pages) وما فيه سيرفر، فالمزامنة تحتاج مخزّناً سحابياً صغيراً باسمك.
الأرخص والأسهل: **Firebase Realtime Database** — مجانية بالكامل عند هذا الحجم، وما تحتاج
بطاقة ولا نشر كود.

> بياناتك تُحفظ محلياً كما هي دائماً. المزامنة طبقة إضافية فوقها، وإيقافها لا يمس بياناتك المحلية.

---

## ١) أنشئ قاعدة البيانات

1. افتح <https://console.firebase.google.com> وسجّل دخول بحساب Google.
2. **Add project** → أي اسم (مثلاً `cfa-tracker`) → عطّل Google Analytics → **Create project**.
3. من القائمة الجانبية: **Build → Realtime Database** → **Create Database**.
4. اختر أقرب موقع (مثلاً `europe-west1`)، ثم اختر **Start in locked mode** → **Enable**.
5. فوق الجدول بيظهر رابط بهذا الشكل — انسخه:

   ```
   https://cfa-tracker-default-rtdb.europe-west1.firebasedatabase.app
   ```

## ٢) اضبط قواعد الوصول

من نفس الصفحة: تبويب **Rules**، امسح الموجود والصق هذا بالضبط ثم **Publish**:

```json
{
  "rules": {
    "cfa-sync": {
      "$code": {
        ".read":  "$code.length >= 16",
        ".write": "$code.length >= 16 && newData.hasChildren(['updatedAt','data'])"
      }
    }
  }
}
```

**ماذا تعني:** لا أحد يقدر يقرأ أو يسرد قاعدة البيانات كلها. الوصول فقط لمسار
`cfa-sync/<رمزك>` ولمن يعرف الرمز نفسه. الرمز الذي يولّده الموقع طوله 24 حرفاً
(≈ 10^36 احتمال) فتخمينه غير عملي.

**صارح نفسك بالمقايضة:** هذه الطريقة بدون تسجيل دخول — الأمان مبني على سرّية الرمز
وحده. لا تنشر الرمز ولا رابط الربط في مكان عام. البيانات هنا ملاحظات مذاكرة، لكن
لو تبي حماية بحساب وكلمة مرور فهذا يتطلب طبقة Firebase Auth كاملة (أكبر وأثقل).

## ٣) فعّلها على الجهاز الأول

1. افتح الموقع → قسم **المزامنة بين الأجهزة**.
2. الصق الرابط من الخطوة ١ في **رابط قاعدة البيانات**.
3. اضغط **توليد رمز** (يولّد رمزاً عشوائياً 24 حرفاً).
4. اضغط **تفعيل المزامنة** — لازم تشوف الحالة تتحول إلى **تمت المزامنة**.

## ٤) اربط بقية الأجهزة

**الطريقة السريعة:** على الجهاز الأول اضغط **نسخ رابط ربط جهاز آخر**، أرسل الرابط
لنفسك (واتساب/ملاحظات)، وافتحه على الجهاز الثاني — يضبط نفسه تلقائياً ويسحب بياناتك.

**الطريقة اليدوية:** افتح الموقع على الجهاز الثاني والصق نفس الرابط ونفس الرمز، ثم
**تفعيل المزامنة**.

> رابط الربط يحمل الرمز السري بداخله. لا ترسله في مجموعة عامة، وامسحه بعد الاستخدام.

---

## كيف تشتغل المزامنة

| الحدث | السلوك |
|---|---|
| فتحت الموقع | يسحب من السحابة إن كانت أحدث |
| عدّلت شيئاً | يرفع تلقائياً بعد ٤ ثوانٍ من آخر تعديل |
| رجعت للتبويب / عاد الإنترنت | يزامن فوراً |
| الموقع مفتوح ومرئي | يتحقق كل دقيقتين |
| ضغطت **مزامنة الآن** | يزامن فوراً ويخبرك بالنتيجة |

**وحدة المزامنة هي بياناتك كاملة** (آخر تعديل يفوز)، إلا في حالة واحدة: إذا عدّلت على
جهازين بعد آخر مزامنة، ما يفوز أحدهما بصمت — يظهر لك صندوق تعارض أحمر تختار منه أي
نسخة تُعتمد، والنسخة الثانية تُحفظ احتياطياً في `localStorage` تحت المفتاح
`cfa_l2_pre_sync_backup` قبل أي استبدال.

**نصيحة عملية:** اقفل التبويب على الجهاز الأول قبل ما تفتح الثاني، وبتتفادى التعارض أصلاً.

## استكشاف الأخطاء

| الحالة | السبب المرجّح | الحل |
|---|---|---|
| `تعذّرت المزامنة (PUT 401)` | القواعد لم تُنشر أو الرمز أقصر من 16 حرفاً | أعد الخطوة ٢، وولّد رمزاً جديداً |
| `تعذّرت المزامنة (GET 404)` | الرابط خاطئ | انسخه مرة أخرى من أعلى صفحة Realtime Database |
| `لا يوجد اتصال` | أوفلاين | يزامن تلقائياً عند رجوع الإنترنت |
| `بيانات السحابة غير صالحة` | البيانات السحابية تالفة أو من نسخة غير متوافقة | استورد نسخة احتياطية على هذا الجهاز ثم اضغط **مزامنة الآن** |

## إيقاف المزامنة

**إيقاف المزامنة** → تأكيد. يتوقف الرفع والسحب فوراً، وبياناتك المحلية تبقى كما هي.
لمسح النسخة السحابية نهائياً: احذف عقدة `cfa-sync` من Firebase Console.
