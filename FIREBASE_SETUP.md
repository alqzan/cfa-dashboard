# إعداد Firebase للوحة CFA Personal Coach v6

المزامنة السحابية اختيارية بالكامل. اللوحة تعمل محلياً 100% بدونها — قسم "مزامنة السحابة"
في الصفحة يبقى مخفياً تلقائياً حتى تُنشئ `assets/js/firebase-config.js` بنفسك. هذه الخطوات
كلها يدوية في Firebase Console — لا يمكن لي تنفيذها نيابة عنك.

## 1) إنشاء مشروع Firebase مستقل

1. افتح https://console.firebase.google.com
2. **Add project** → اسمه مثلاً `cfa-l2-coach` (لا تربطه بأي مشروع آخر لديك إلا إذا قررت ذلك بنفسك).
3. عطّل Google Analytics إن لم تكن تحتاجه (اختياري، لا علاقة له باللوحة).

## 2) إنشاء Web App والحصول على الإعدادات

1. من صفحة المشروع الرئيسية اضغط أيقونة **</>** (Web).
2. اسم التطبيق: أي شيء، مثلاً `cfa-dashboard`.
3. لا تفعّل Firebase Hosting هنا — اللوحة تُنشر عبر GitHub Pages، ليس عبر Firebase Hosting.
4. بعد الإنشاء ستظهر لك قيم `firebaseConfig` (apiKey, authDomain, projectId, storageBucket,
   messagingSenderId, appId). احتفظ بهذه الصفحة مفتوحة للخطوة التالية.

## 3) تفعيل Email/Password Authentication

1. من القائمة الجانبية: **Build → Authentication → Get started**.
2. تبويب **Sign-in method** → فعّل **Email/Password** (الخيار الأول فقط، ليس Email link).
3. **لا تفعّل Anonymous** — تعمّدنا عدم استخدامه لأنه يعطي كل جهاز UID مختلف، فتفقد المزامنة معناها.

## 4) إنشاء Cloud Firestore

1. **Build → Firestore Database → Create database**.
2. اختر أي موقع جغرافي قريب منك (لا يمكن تغييره لاحقاً).
3. ابدأ بوضع **production mode** (سنضبط القواعد الصحيحة في الخطوة التالية على أي حال).

## 5) نشر Firestore Rules

الملف `firestore.rules` في جذر هذا المستودع جاهز — يمنع أي مستخدم من قراءة أو كتابة بيانات
مستخدم آخر (`request.auth.uid` يجب أن يطابق `uid` في المسار).

**الطريقة السهلة (بدون Firebase CLI):**
1. من Firebase Console: **Firestore Database → Rules**.
2. الصق محتوى `firestore.rules` كاملاً مكان القواعد الافتراضية.
3. **Publish**.

**أو عبر Firebase CLI (إن كان مثبّتاً لديك):**
```bash
npm install -g firebase-tools
firebase login
firebase init firestore   # اختر المشروع الذي أنشأته، واستخدم firestore.rules الموجود
firebase deploy --only firestore:rules
```

## 6) ملء إعدادات اللوحة محلياً

```bash
cp assets/js/firebase-config.example.js assets/js/firebase-config.js
```

افتح `assets/js/firebase-config.js` والصق القيم الست من الخطوة 2. هذا الملف **غير مُتتبَّع في
Git** (موجود في `.gitignore`) — لن يُرفع للمستودع، وكل جهاز/نسخة تحتاج نسخته الخاصة منه.

بعد حفظ الملف، أعد تحميل `index.html` — سيظهر قسم "مزامنة السحابة (Firebase)" تلقائياً.

> ملاحظة أمان: قيم `firebaseConfig` (apiKey ...) ليست سرّية بطبيعة تطبيقات الويب — الحماية
> الحقيقية تأتي من تفعيل Authentication وقواعد Firestore في الخطوتين 3 و5، وليس من إخفاء هذا
> الملف. أبقيناه خارج المستودع فقط لأنك طلبت عدم نشر رقم مشروعك علناً على GitHub.

## 7) اختبار المزامنة بجهازين (أو متصفحين)

1. افتح اللوحة في متصفحين مختلفين (أو جهاز ثانٍ)، وسجّل الدخول بنفس البريد وكلمة المرور في الاثنين.
2. سجّل أي تعديل في الأول (مثلاً حدّث حالة قراءة) واضغط **مزامنة الآن**.
3. في الثاني اضغط **مزامنة الآن** — يجب أن يظهر نفس التعديل.
4. عدّل حقلاً نصياً (مثل ملاحظة قراءة) في الجهازين في نفس الوقت قبل المزامنة، ثم زامن الاثنين —
   يجب أن تظهر رسالة "يوجد تعارض" وتُحفظ نسختا النص معاً بدل فقد إحداهما.
5. جرّب أول ربط لحساب لديه بيانات سحابية سابقة من جهاز آخر — يجب أن تظهر لك ثلاثة خيارات:
   استخدام البيانات المحلية / استخدام بيانات السحابة / دمج النسختين.

## القيود الحالية (بصراحة)

- الدمج يقارن "متى تغيّر الجهاز آخر مرة" ككل، وليس حقلاً حقلاً — للحقول النصية الطويلة (الملاحظات،
  Brief) هذا كافٍ لأنه لا يفقد أي نسخة عند التعارض. للحقول الرقمية الصغيرة (نتيجة محاكاة، جلسة
  مؤقّت) يُفضَّل الطرف الذي حفظ محلياً عند التعارض الحقيقي، مع تسجيل الحدث كتعارض في كل الأحوال.
- لم أتمكّن من اختبار تسجيل الدخول والمزامنة الفعلية ضد مشروع Firebase حي داخل بيئة التطوير هذه
  (لا يوجد وصول شبكي لإنشاء مشروع أو الاتصال بـ Firestore من هنا) — اختبرت منطق الدمج بوحدات
  اختبار آلية منفصلة عن الشبكة (`tests/sync-merge.test.mjs`)، وتحقّقت أن اللوحة تعمل بسلام تماماً
  بدون Firebase وأنها لا تتعطّل حتى مع وجود `firebase-config.js` لو تعذّر الوصول لخوادم Firebase.
  الخطوة 7 أعلاه هي الاختبار الحقيقي المتبقّي عليك بعد إنشاء مشروعك.
